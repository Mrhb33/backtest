{
  "name": "bollinger-bands-strategy",
  "version": "1.0.0",
  "description": "Bollinger Bands mean reversion strategy for WASM backtesting",
  "main": "dist/bollinger_bands_strategy.js",
  "types": "dist/bollinger_bands_strategy.d.ts",
  "scripts": {
    "build": "tsc && wasm-pack build --target web --out-dir pkg",
    "build:node": "tsc && wasm-pack build --target nodejs --out-dir pkg-node",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "wasm-pack": "^0.12.0"
  },
  "dependencies": {
    "wasm-bindgen": "^0.2.0"
  },
  "keywords": [
    "trading",
    "strategy",
    "backtesting",
    "wasm",
    "bollinger-bands",
    "mean-reversion"
  ],
  "author": "backtest-engine",
  "license": "MIT",
  "files": [
    "dist/",
    "pkg/",
    "README.md"
  ]
}

